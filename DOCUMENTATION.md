# SproutFTP - เอกสารประกอบระบบและคู่มือการใช้งาน

## ภาพรวมระบบ

**SproutFTP** เป็นเว็บแอปพลิเคชันสำหรับจัดการไฟล์ผ่าน FTP Server ที่พัฒนาด้วย ASP.NET Core Razor Pages และใช้ไลบรารี FluentFTP สำหรับการเชื่อมต่อกับ FTP Server

### คุณสมบัติหลัก
- อัปโหลดไฟล์ไปยัง FTP Server
- ดาวน์โหลดไฟล์จาก FTP Server
- ลบไฟล์จาก FTP Server
- แสดงรายการไฟล์และโฟลเดอร์
- รองรับการเชื่อมต่อแบบ IPv4 เท่านั้น
- ปิดการใช้งาน SSL เพื่อหลีกเลี่ยงปัญหาการเชื่อมต่อ

## โครงสร้างโปรเจกต์

```
SproutFTP/
├── Program.cs              # ไฟล์คอนฟิกและเริ่มต้นแอปพลิเคชัน
├── appsettings.json        # การตั้งค่า FTP และการเชื่อมต่อ
├── Pages/
│   ├── Index.cshtml        # หน้าเว็บหลัก (Frontend)
│   └── Index.cshtml.cs     # Logic สำหรับหน้า Index (Backend)
├── docker-compose.yml      # คอนฟิก Docker สำหรับ FTP Server
└── README.md              # คำอธิบายเบื้องต้น
```

## การติดตั้งและเริ่มต้นใช้งาน

### ข้อกำหนดเบื้องต้น
- .NET 10.0 SDK ขึ้นไป
- Docker (สำหรับรัน FTP Server)

### ขั้นตอนการติดตั้ง

1. **ดึงโปรเจกต์**
   ```bash
   git clone <repository-url>
   cd SproutFTP
   ```

2. **รัน FTP Server ด้วย Docker**
   ```bash
   docker-compose up -d
   ```
   - FTP Server จะรันที่ `127.0.0.1:21`
   - ผู้ใช้: `admin`
   - รหัสผ่าน: `1234`

3. **รันเว็บแอปพลิเคชัน**
   ```bash
   dotnet run
   ```
   - เว็บแอปจะรันที่ `http://localhost:5000`

## การตั้งค่า

### การตั้งค่า FTP (ใน appsettings.json)

```json
{
  "FtpSettings": {
    "Host": "127.0.0.1",
    "User": "admin", 
    "Pass": "1234"
  }
}
```

### การตั้งค่า Docker FTP Server (ใน docker-compose.yml)

```yaml
services:
  ftp-server:
    image: delfer/alpine-ftp-server
    container_name: sprout-ftp-server
    ports:
      - "21:21"
      - "21100-21110:21100-21110"
    environment:
      - USERS=admin|1234
      - ADDRESS=127.0.0.1
      - MIN_PORT=21100
      - MAX_PORT=21110
```

## การใช้งาน

### 1. อัปโหลดไฟล์
1. เปิดเว็บแอปที่ `http://localhost:5000`
2. คลิกช่อง "Choose File" เพื่อเลือกไฟล์
3. คลิกปุ่ม "Upload"
4. ไฟล์จะถูกอัปโหลดไปยัง FTP Server

### 2. ดาวน์โหลดไฟล์
1. ดูรายการไฟล์ในตารางด้านล่าง
2. คลิกปุ่ม "Download" ที่อยู่ข้างชื่อไฟล์
3. ไฟล์จะถูกดาวน์โหลดไปยังเครื่องผู้ใช้

### 3. ลบไฟล์
1. ดูรายการไฟล์ในตารางด้านล่าง
2. คลิกปุ่ม "Delete" ที่อยู่ข้างชื่อไฟล์
3. ยืนยันการลบในกล่องยืนยัน
4. ไฟล์จะถูกลบออกจาก FTP Server

## การทำงานของระบบ

### หน้า Index (Pages/Index.cshtml.cs)

#### ฟังก์ชันหลัก

1. **GetFtpClient()**
   - สร้างการเชื่อมต่อ FTP Client
   - ตั้งค่าให้ใช้ IPv4 เท่านั้น
   - ปิดการใช้งาน SSL
   - ตั้งค่า Timeout 10 วินาที

2. **LoadFileList()**
   - เชื่อมต่อกับ FTP Server
   - ดึงรายการไฟล์และโฟลเดอร์
   - จัดการข้อผิดพลาดและแสดงข้อความ error

3. **OnGetAsync()**
   - Handler สำหรับการเข้าถึงหน้าเว็บ
   - เรียก LoadFileList() เพื่อโหลดรายการไฟล์

4. **OnPostUploadAsync()**
   - Handler สำหรับการอัปโหลดไฟล์
   - ตรวจสอบว่ามีการเลือกไฟล์หรือไม่
   - อัปโหลดไฟล์ไปยัง FTP Server
   - รีเฟรชหน้าเว็บหลังอัปโหลดสำเร็จ

5. **OnGetDownloadAsync()**
   - Handler สำหรับการดาวน์โหลดไฟล์
   - ดาวน์โหลดไฟล์จาก FTP Server เป็น byte array
   - ส่งไฟล์กลับไปยังผู้ใช้

6. **OnPostDeleteAsync()**
   - Handler สำหรับการลบไฟล์
   - ลบไฟล์จาก FTP Server
   - รีเฟรชหน้าเว็บหลังลบสำเร็จ

### หน้าเว็บ (Pages/Index.cshtml)

#### ส่วนประกอบหลัก

1. **ส่วนหัว**
   - แสดงชื่อแอปพลิเคชัน
   - แสดงข้อความ error ถ้ามี

2. **ส่วนอัปโหลด**
   - ฟอร์มสำหรับเลือกและอัปโหลดไฟล์
   - ใช้ `enctype="multipart/form-data"` สำหรับการอัปโหลดไฟล์

3. **ส่วนแสดงรายการไฟล์**
   - ตารางแสดงรายการไฟล์และโฟลเดอร์
   - แสดงชื่อไฟล์ ขนาด และวันที่แก้ไขล่าสุด
   - มีปุ่ม Download และ Delete สำหรับแต่ละไฟล์

## การแก้ไขปัญหา

### ปัญหาการเชื่อมต่อ
- ตรวจสอบว่า FTP Server รันอยู่หรือไม่
- ตรวจสอบการตั้งค่า Host, User, Pass ใน appsettings.json
- ตรวจสอบว่า Docker รัน FTP Server ได้หรือไม่

### ปัญหาการอัปโหลด/ดาวน์โหลด
- ตรวจสอบขนาดไฟล์ (ขนาดใหญ่มากอาจใช้เวลานาน)
- ตรวจสอบการตั้งค่า Timeout ใน GetFtpClient()
- ตรวจสอบการตั้งค่าพอร์ต Passive Mode ใน Docker

### ปัญหา Permission
- ตรวจสอบว่าผู้ใช้ FTP มีสิทธิ์ในการอ่าน/เขียนไฟล์
- ตรวจสอบการตั้งค่า Permission ใน Docker Volume

## การพัฒนาต่อยอด

### เพิ่มคุณสมบัติ
- การสร้าง/ลบโฟลเดอร์
- การเปลี่ยนชื่อไฟล์
- การอัปโหลดหลายไฟล์พร้อมกัน
- การค้นหาและกรองไฟล์
- การแสดง Preview ไฟล์ (รูปภาพ เอกสาร)

### การปรับปรุงความปลอดภัย
- เพิ่มการตรวจสอบสิทธิ์ผู้ใช้
- เพิ่มการเข้ารหัสการสื่อสาร
- เพิ่มการบันทึก Log กิจกรรม

### การปรับปรุงประสิทธิภาพ
- เพิ่มการ Cache รายการไฟล์
- เพิ่มการอัปโหลดแบบ Chunk
- เพิ่มการดาวน์โหลดแบบ Parallel

## ข้อมูลการติดต่อ

ผู้พัฒนา: นายวีรพงศ์ วสุมงคลพจน์  
รหัสนักศึกษา: 670112418022  
สาขา: เทคโนโลยีสารสนเทศ หมู่ 1